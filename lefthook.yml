pre-commit:
  commands:
    typecheck:
      run: npx tsc --noEmit

pre-push:
  parallel: true
  commands:
    lint:
      run: npm run lint
    build:
      run: npm run build
    test:
      run: npm run test
